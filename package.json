{
  "name": "typescript-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "./node_modules/.bin/concurrently 'cd packages/components && yarn' 'cd packages/server && yarn' 'cd packages/web && yarn' 'cd packages/api && yarn'"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
